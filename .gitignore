# Secrets / credentials — never commit
# (bare name matches at any depth; explicit path documents where it lives now)
config.json
config/config.json
config.local.json
*.local.json

# Runtime logs — local operational history, not part of the repo
logs/

# Python
__pycache__/
*.py[cod]
.venv/
venv/

# Node
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Claude local settings (per-machine overrides)
.claude/settings.local.json

# OS / editor cruft
.DS_Store
Thumbs.db
.vscode/
.idea/
